/**
 * Returns the provided value unchanged.
 *
 * This function takes a value as input and returns it unchanged. It is a simple
 * identity function, useful for testing or when you need to pass a value without
 * any modification.
 *
 * @param any value - The value to be returned.
 * @returns {any} The input value, unchanged.
 */

const checkValue = (value) => {
    return value;
};

export default checkValue;
